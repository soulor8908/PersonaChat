## 2026-07-06

### 项目诊断分析
- 卡帕西视角全面分析了 PersonaChat 项目优缺点，生成诊断报告在 `~/.workbuddy/plans/blazing-forging-newton.md`
- 总评 78/100：架构设计 90、代码质量 75、测试 45、前端体验 70、工程化 85、文档 80、安全 65
- 优点：SSOT 契约层、22 项 machine-enforced 门禁、Token 封闭设计、增量上下文系统、小程序 SSE 流式
- 缺点：API 零测试(P0)、全局可变状态(P1)、每次请求建表(P1)、类型绕过(P1)、Rate-limit 内存态、前端竞态
- 路线图：P0(测试,3-4轮) → P1(技术债,1轮) → P2(类型,1-2轮) → P3(微调,1轮) → P4(上线加固)

### P0 完成：API 测试覆盖
- 新增 16 个测试文件，248 个新增测试 + 71 个已有 = **319 总测试**
- 覆盖率从 0 提升到 4 层全覆盖 (domain/middleware/service/router)
- Trinity 全绿：typecheck ✅ | check-rules 16/16 ✅ | tests 319/319 ✅
- 新增回溯 Spec：docs/spec/backrefactor-p0-test-coverage.md
- 测试分布：
  - domain: llm 26 tests, tool-executor 33, memory 14, persona-parser 19 = 92
  - middleware: auth 7, cors 9, error-handler 6, rate-limit 9, security-headers 5, body-limit 4 = 40
  - service: chat-svc 29+10, persona-svc 17 = 56
  - router: chat 16, persona 19+5, extra 5 = 45
  - contracts (已有): 11, web (已有): 46, e2e (已有): 2
  - 其他已有: 17 = 67 (circa)
- 文件拆分：chat-svc.test.ts → 主文件 + chat-svc-ops.test.ts + __chat-test-utils.ts 共享工具
- 修复：CODE-002b 假阳性 (eval 字符串), CODE-004 超大文件拆分, AI-001 Spec 回溯补齐

### P1 完成：消除全局状态 + 类型绕过
- **llm.ts**: 移除 `let llmLogFn` 模块级可变状态 + `setLLMLogger()` setter
  - `callLLM()` 新增第4参数 `onLog?: LLMLogFn`，直接注入
  - 3 处 `llmLogFn?.()` → `onLog?.()`
- **server.ts**: 
  - `env as unknown as Record<string, string | undefined>` → 显式提取 string key 构造 env 对象，消除双重断言
  - `setLLMLogger(...)` → 构造 `onLog` 回调传入 `ChatServiceOptions.onLog`
  - `ensureTables`: 添加 `let tablesInitialized = false` 标记，跳过后续请求的建表 DB 往返
- **service/chat-svc.ts**: 重构 constructor 为 `ChatServiceOptions` 结构体
  - `(repo, repo, env, memoryRepo?, toolCtx?)` → `(repo, repo, opts: ChatServiceOptions)`
  - `onLog` 通过 opts 传入，透传到 3 处 `callLLM()` + `chat-helpers.extractMemoriesAsync()`
- **service/chat-helpers.ts**: `extractMemoriesAsync` 新增 `onLog?: LLMLogFn` 参数
- **测试更新**: `__chat-test-utils.ts` setupMocks 适配新构造函数；`chat-svc.test.ts` + `chat-svc-ops.test.ts` + `chat.e2e.test.ts` + `chat-svc.test.ts(test/)` 全部适配
- Trinity 全绿：typecheck ✅ | check-rules 16/16 ✅ | tests 318/318 ✅
