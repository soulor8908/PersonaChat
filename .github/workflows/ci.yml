# PersonaChat CI — AI-Native 门禁管线
# 适配技术栈: Hono + CF Workers + 微信小程序 + pnpm workspace

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  trinity:
    name: trinity (typecheck + rules + lint + test)
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # G0 门禁需要 base ref 历史以执行 git diff

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Rule check
        run: pnpm check
        env:
          # G0 流程门禁: PR 场景比较 base ref 与 HEAD，触发 AI-001/002/003/007
          # push 事件不设 BASE_REF → 工作树 vs HEAD 比较（推到 main 后工作树干净 → advisory skip）
          BASE_REF: ${{ github.event_name == 'pull_request' && format('origin/{0}', github.base_ref) || '' }}
        # check-rules.mjs — 22 项 enforcement（含 G0 流程门禁），阻断项不通过则 CI 红

      - name: Lint
        run: pnpm lint

      - name: Unit tests
        run: pnpm test

  spec-binding:
    name: spec-binding (Dx <-> code)
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Spec-Binding check
        run: node scripts/check-spec-binding.mjs

  retro-gate:
    name: retro (G8 gate)
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Retro index check (META-005)
        run: node scripts/gen-retro-index.mjs

  smoke-test:
    name: smoke-test (health + models)
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install + test
        run: pnpm install --frozen-lockfile && pnpm dev:api &
        shell: bash

      - name: Wait + smoke
        run: sleep 5 && node scripts/smoke-test.mjs
        shell: bash

  frontend-test:
    name: frontend (vitest + jsdom)
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Frontend unit tests (jsdom)
        working-directory: apps/web
        run: pnpm test

      - name: Frontend lint (Tailwind class checks)
        working-directory: apps/web
        run: pnpm lint

  deploy-staging:
    name: deploy-staging
    if: github.ref == 'refs/heads/main'
    needs: [trinity, spec-binding, retro-gate, frontend-test, smoke-test]
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Deploy to staging
        run: npx wrangler deploy --env production
        working-directory: apps/api
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
